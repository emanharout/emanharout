---
title: Blog
layout: template-parts/base.njk
---

<h1>Eman's Blog</h1>

<ul class="blog-list">
{%- for post in collections.post | reverse -%}
    <li>
        <article>
            {# TODO: Add image: /path/to/image.jpg in each post's frontmatter #}
            {# <img src="{{ post.data.image }}" alt="{{ post.data.imageAlt }}" class="featured-image"/> #}
            <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
            <p class="post-metadata">by {{ post.data.author }} on {{ post.date | postDate }}</p>
            <p class="post-description">{{ post.data.description }}</p>
        </article>
    </li>
{%- endfor -%}
</ul>